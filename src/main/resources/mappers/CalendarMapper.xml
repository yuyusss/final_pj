<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.project.board.dao.ICalendarDAO">
    <resultMap id="calenResult" type="com.project.board.model.CalendarVO">
        <result property="calNo" column="calNo" />
        <result property="numTitle" column="numTitle" />
        <result property="numURL" column="numURL" />
    </resultMap>

    <!-- 전체 넘버 조회
    <select id="listAllMedia" resultMap="calenResult">
        SELECT * FROM number ORDER BY numNo DESC
    </select>  -->
    
</mapper>
  
  