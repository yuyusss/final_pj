<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="com.project.board.dao.ITicketbookDAO">

	<resultMap id="listAllTicketbook" type="com.project.board.model.TicketbookVO">
			<result property="no" column="no"/>
			
	</resultMap>
	
	<!--  전체 홀 조회  -->
	<!--<select id="listAllTicketbook" resultMap="Ticketbook" resultType="com.project.board.model.TicketbookVO">
			SELECT * FROM tk
	</select>-->
	<!-- 
	<select id="getMusicalByGenre" resultType="map" parameterType="string">
        SELECT muscNo, muscImg FROM musical WHERE muscGenre = #{genre} 
    </select> -->
	
	<select id="getMusicalByGenre" resultType="com.project.board.model.MusicalByGenreVO" parameterType="string">
        SELECT e.memId, e.genre, m.muscNo, m.muscGenre, m.muscImg FROM member e LEFT JOIN musical m ON e.genre = m.muscGenre WHERE e.memId = #{memId} limit 3
    </select>
	
	
</mapper>